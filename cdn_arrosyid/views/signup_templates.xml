<odoo>
    <template id="signup" name="Sign Up">
        <t t-call="website.layout">
            <div id="wrap" class="oe_structure oe_empty">
            <t t-call="auth_signup.fields"/>
            </div>
        </t>
    </template>
    <template id="auth_signup.fields" name="Sign Up">
        <form class="oe_signup_form" role="form" t-if="not message">
            <input type="hidden" name="csrf_token" t-att-value="csrf_token" hidden="1"/>
            <div class="container m-2">
                <h5 class="">Register</h5>
                <div class="mb-3 field-login">
                    <label for="email">Email</label>
                    <input type="text" name="email" id="email" class="form-control form-control-sm" autofocus="autofocus"
                        autocapitalize="off" required="required" t-att-readonly="'readonly' if only_passwords else None" />
                </div>

                <div class="mb-3 field-name">
                    <label for="name">Nama</label>
                    <input type="text" name="name" id="name" class="form-control form-control-sm" placeholder="e.g. John Doe"
                        required="required" t-att-readonly="'readonly' if only_passwords else None"
                        t-att-autofocus="'autofocus' if login and not only_passwords else None"  />
                </div>

                <div class="mb-3 field-password pt-2">
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" class="form-control form-control-sm"
                        required="required" t-att-autofocus="'autofocus' if only_passwords else None"/>
                </div>

                <div class="mb-3 field-confirm_password">
                    <label for="confirm_password">Confirm Password</label>
                    <input type="password" name="confirm_password" id="confirm_password" class="form-control form-control-sm" required="required" />
                </div>
                
                <button class="btn btn-primary" type="submit" id="submit_signup">Daftar</button>
            </div>
        </form>
    </template>
</odoo>